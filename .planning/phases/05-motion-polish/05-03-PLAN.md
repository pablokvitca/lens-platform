---
phase: 05-motion-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web_frontend/src/components/module/NarrativeChatSection.tsx
  - web_frontend/src/components/module/MarkCompleteButton.tsx
  - web_frontend/src/components/module/StageProgressBar.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All interactive buttons have active:scale-95 visual feedback"
    - "Touch feedback is consistent across chat, progress bar, and completion buttons"
  artifacts:
    - path: "web_frontend/src/components/module/NarrativeChatSection.tsx"
      provides: "Touch feedback on send, mic, and stop buttons"
      contains: "active:scale-95"
    - path: "web_frontend/src/components/module/MarkCompleteButton.tsx"
      provides: "Touch feedback on completion buttons"
      contains: "active:scale-95"
    - path: "web_frontend/src/components/module/StageProgressBar.tsx"
      provides: "Touch feedback on navigation arrows and dots"
      contains: "active:scale-95"
  key_links:
    - from: "All touch targets"
      to: "Visual feedback"
      via: "Tailwind active: modifier"
      pattern: "active:scale-95"
---

<objective>
Add active:scale-95 touch feedback to all interactive elements that currently lack visual press response.

Purpose: Close Gap 1 from verification - inconsistent touch feedback creates unpolished UX where some buttons scale on press and others don't.
Output: All touch targets provide immediate visual feedback on press for consistent tactile response.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-motion-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add touch feedback to chat buttons</name>
  <files>web_frontend/src/components/module/NarrativeChatSection.tsx</files>
  <action>
Add active:scale-95 and transition-all to the chat interface buttons:

1. **Microphone button** (~line 726): Add `active:scale-95` to existing className
2. **Stop recording button** (~line 776): Add `active:scale-95` to existing className
3. **Send button** (~line 793): Add `active:scale-95` to existing className

Each button already has `transition-colors` - change to `transition-all` to include transform.

Pattern from 05-01:
```tsx
className="... transition-all active:scale-95 ..."
```
  </action>
  <verify>grep -n "active:scale-95" web_frontend/src/components/module/NarrativeChatSection.tsx shows 3 matches</verify>
  <done>Send, mic, and stop buttons all scale down on press</done>
</task>

<task type="auto">
  <name>Task 2: Add touch feedback to completion buttons</name>
  <files>web_frontend/src/components/module/MarkCompleteButton.tsx</files>
  <action>
Add active:scale-95 and transition-all to both buttons in MarkCompleteButton:

1. **Next section button** (~line 32): Add `active:scale-95` and change `transition-colors` to `transition-all`
2. **Mark section complete button** (~line 58): Add `active:scale-95` and change `transition-colors` to `transition-all`

Both buttons already have hover states - add active states for touch.
  </action>
  <verify>grep -n "active:scale-95" web_frontend/src/components/module/MarkCompleteButton.tsx shows 2 matches</verify>
  <done>Mark Complete and Next Section buttons scale on press</done>
</task>

<task type="auto">
  <name>Task 3: Add touch feedback to stage progress bar</name>
  <files>web_frontend/src/components/module/StageProgressBar.tsx</files>
  <action>
Add active:scale-95 to navigation arrows and stage dots:

1. **Previous button** (~line 119): Add `active:scale-95` to className
2. **Stage dots** (~line 174): Add `active:scale-95` to the button className within the map
3. **Next button** (~line 205): Add `active:scale-95` to className

All elements already have `transition-all` or `transition-colors` - ensure transition-all is present.

Use scale-95 (5%) for all elements to match other buttons established in 05-01.
  </action>
  <verify>grep -n "active:scale-95" web_frontend/src/components/module/StageProgressBar.tsx shows 3+ matches (prev, dots, next)</verify>
  <done>Progress bar arrows and dots all have touch feedback</done>
</task>

</tasks>

<verification>
1. All modified files contain active:scale-95 classes
2. Build passes: `cd web_frontend && npm run build`
3. Manual test: pressing any button shows immediate scale-down feedback
</verification>

<success_criteria>
- MOTION-03 requirement satisfied: Interactive elements respond to touch with immediate feedback
- VISUAL-02 requirement satisfied: Consistent touch feedback patterns across all interactive elements
- Verification Truth #3 passes: "Buttons and interactive elements respond immediately to touch"
</success_criteria>

<output>
After completion, create `.planning/phases/05-motion-polish/05-03-SUMMARY.md`
</output>
